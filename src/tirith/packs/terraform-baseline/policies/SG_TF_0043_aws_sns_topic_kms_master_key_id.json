{
  "meta": {
    "version": "v1",
    "required_provider": "stackguardian/terraform_plan",
    "id": "SG_TF_0043",
    "name": "Ensure all data stored in the SNS topic is encrypted",
    "description": "Ensure all data stored in the SNS topic is encrypted",
    "tags": [
      "cloud:aws",
      "encryption"
    ],
    "remediation": "Set kms_master_key_id on every aws_sns_topic."
  },
  "evaluators": [
    {
      "id": "sns_topic_kms_master_key",
      "description": "kms_master_key_id must be a non-empty string on every aws_sns_topic. The attribute is optional, so an unset key arrives as an explicit null in change.after and fails IsNotEmpty.",
      "provider_args": {
        "operation_type": "attribute",
        "terraform_resource_type": "aws_sns_topic",
        "terraform_resource_attribute": "kms_master_key_id"
      },
      "condition": {
        "type": "IsNotEmpty",
        "error_tolerance": 1
      }
    }
  ],
  "eval_expression": "sns_topic_kms_master_key"
}
